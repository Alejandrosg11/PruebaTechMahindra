import React, { useEffect, useState } from "react";

export const MatchGame = ( data: object ) => {
    const [countryList] = useState(data)

    console.log(countryList.data)

    return(
        <>

        </>
    )
}